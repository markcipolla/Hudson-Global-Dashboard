<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:buildservers, :index) %></li>
      <li class="active"><%= link_to pat(:new), url(:buildservers, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:new) %> <% mt(:buildserver) %></h2>
    <div class="inner">
      <% form_for :buildserver, url(:buildservers, :create), :class => :form do |f| %>
        <%= partial "buildservers/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>