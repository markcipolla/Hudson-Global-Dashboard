<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:buildservers, :index) %></li>
      <li><%= link_to pat(:new), url(:buildservers, :new) %></li>
      <li class="active"><%= link_to pat(:edit), url(:buildservers, :edit, :id => @buildserver.id) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:edit) %> <% mt(:buildserver) %></h2>
    <div class="inner">
      <% form_for :buildserver, url(:buildservers, :update, :id => @buildserver.id), :method => :put, :class => :form do |f| %>
        <%= partial "buildservers/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>